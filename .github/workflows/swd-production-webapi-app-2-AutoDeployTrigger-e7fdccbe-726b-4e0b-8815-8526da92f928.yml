name: Trigger auto deployment for swd-production-webapi-app-2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/swd-production-webapi-app-2-AutoDeployTrigger-e7fdccbe-726b-4e0b-8815-8526da92f928.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.SWDPRODUCTIONWEBAPIAPP2_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: ghcr.io
          registryUsername: ${{ secrets.SWDPRODUCTIONWEBAPIAPP2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SWDPRODUCTIONWEBAPIAPP2_REGISTRY_PASSWORD }}
          containerAppName: swd-production-webapi-app-2
          resourceGroup: swd-project-resource-group
          imageToBuild: ghcr.io/office-hour/ev-rental-be:${{ github.sha }}
          
            

     

