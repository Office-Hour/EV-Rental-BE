@page
@model WebApp.Areas.Staff.Pages.Rentals.CreateModel
@{
    ViewData["Title"] = "Tạo Rental";
}

<link rel="stylesheet" href="~/css/staff-shared.css" asp-append-version="true" />
<link rel="stylesheet" href="~/css/staff-rentals.css" asp-append-version="true" />

<!-- Hero Section -->
<section class="py-5 staff-hero">
    <div class="container staff-container">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
            <div class="text-white">
                <h1 class="fw-bold mb-2">🚗 Tạo Rental</h1>
                <p class="fs-5 text-white-50 mb-0">Tạo hợp đồng thuê xe cho khách hàng</p>
            </div>
            <a asp-area="Staff" asp-page="/Bookings/Details" asp-route-id="@Model.BookingId" class="btn btn-outline-light rounded-pill px-4 py-2">
                ← Quay Lại
            </a>
        </div>
    </div>
</section>

<section class="py-5 bg-light">
    <div class="container staff-container">

        @if (Model.Booking != null)
        {
            <div class="row g-4">
                <div class="col-md-8">
                    <div class="bg-white p-4 rounded-3 shadow-lg">
                        <div class="d-flex align-items-center gap-2 mb-4 pb-3 border-bottom">
                            <h2 class="fw-semibold text-dark mb-0">📝 Thông Tin Rental</h2>
                        </div>
                        <div>
                            <form method="post">
                                <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-2"></div>

                                <div class="mb-4">
                                    <label class="form-label fw-semibold text-dark">Booking ID</label>
                                    <input type="text" class="form-control rounded-2" value="@Model.BookingId" readonly />
                                </div>

                                <div class="row g-3 mb-4">
                                    <div class="col-md-6">
                                        <label asp-for="Input.StartTime" class="form-label fw-semibold text-dark">
                                            🕐 Thời Gian Bắt Đầu *
                                        </label>
                                        <input asp-for="Input.StartTime" type="datetime-local" class="form-control rounded-2" />
                                        <span asp-validation-for="Input.StartTime" class="text-danger"></span>
                                    </div>

                                    <div class="col-md-6">
                                        <label asp-for="Input.EndTime" class="form-label fw-semibold text-dark">
                                            📅 Thời Gian Kết Thúc *
                                        </label>
                                        <input asp-for="Input.EndTime" type="datetime-local" class="form-control rounded-2" />
                                        <span asp-validation-for="Input.EndTime" class="text-danger"></span>
                                    </div>
                                </div>

                                <div class="d-grid">
                                    <button type="submit" class="btn btn-success rounded-pill py-3">
                                        ➕ Tạo Rental
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="bg-white p-4 rounded-3 shadow-lg staff-card-accent-top-success">
                        <div class="d-flex align-items-center gap-2 mb-4 pb-3 border-bottom">
                            <h2 class="fw-semibold text-dark mb-0 fs-5">ℹ️ Thông Tin Booking</h2>
                        </div>
                        <div class="d-grid gap-3">
                            <div class="p-3 bg-light rounded-2">
                                <small class="text-muted d-block mb-1">Renter ID</small>
                                <code class="fw-bold">@Model.Booking.RenterId</code>
                            </div>
                            
                            <div class="p-3 bg-light rounded-2">
                                <small class="text-muted d-block mb-2">Thời Gian</small>
                                <div class="text-dark" style="font-size: 0.875rem;">
                                    <div class="mb-1">
                                        <strong>🕐</strong> @Model.Booking.StartTime.ToString("dd/MM/yyyy HH:mm")
                                    </div>
                                    <div>
                                        <strong>📅</strong> @Model.Booking.EndTime.ToString("dd/MM/yyyy HH:mm")
                                    </div>
                                </div>
                            </div>
                            
                            <div class="p-3 bg-light rounded-2">
                                <small class="text-muted d-block mb-1">Vehicle At Station</small>
                                <code class="fw-bold">@Model.Booking.VehicleAtStationId</code>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
